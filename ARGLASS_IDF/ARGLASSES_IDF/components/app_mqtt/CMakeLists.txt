# =====================================================================
# MQTT 客户端模块 CMakeLists.txt (遗留/未使用)
# =====================================================================
# ⚠️ 此模块为遗留代码，已被 WebSocket 替代。
# main.c 中不再依赖此组件。
#
# 源文件: app_mqtt.c
# 依赖:
#   - mqtt:     ESP-IDF MQTT 客户端库
#   - esp_netif: 网络接口
# =====================================================================

idf_component_register(SRCS "app_mqtt.c"
                       INCLUDE_DIRS "."
                       REQUIRES mqtt esp_netif)
