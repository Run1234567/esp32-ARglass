# =====================================================================
# PDM I2S 麦克风模块 CMakeLists.txt
# =====================================================================
# 源文件: audio_app.c
# 依赖: log (日志), driver (I2S PDM 驱动)
# =====================================================================

idf_component_register(
    SRCS "audio_app.c"
    INCLUDE_DIRS "."
    REQUIRES log esp_driver_i2s
)
